#include <STDIO.H>
int main()
{
    int arr[] = {1, 2, 3, 4, 5};
    int pos = 2;
    int size = 5;

    for (int i = size; i > pos; i--)
    {
        arr[pos] = 
    }
    size--;

    for (int i = 0; i < size; i++)
    {
        printf("%d \t", arr[i]);
    }
    return 0;
}